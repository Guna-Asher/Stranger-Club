import { Check, LoaderCircle, X } from 'lucide-react';

export default function Proof({ player, close, confirm, reject, busy }) { return <div className="overlay"><section className="sheet"><button className="close" onClick={close}><X /></button><p className="eyebrow">PAYMENT REVIEW</p><h2>{player.name}</h2><strong className="amount">₹{player.payment.amount}</strong><img src={player.payment.screenshot_url} alt="Payment proof" /><p className="quiet">Submitted {new Intl.DateTimeFormat('en-IN', { dateStyle: 'medium', timeStyle: 'short' }).format(new Date(player.payment.submitted_at))}</p><div className="review-actions"><button className="ghost-button" onClick={reject} disabled={busy}>REJECT</button><button className="primary-button" onClick={confirm} disabled={busy}>{busy ? <LoaderCircle className="spin" /> : 'CONFIRM PAYMENT'}<Check /></button></div></section></div>; }
